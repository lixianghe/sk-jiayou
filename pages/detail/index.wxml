<Detail-one id="detail" wx:if="{{layoutType === 'typeOne'}}" detail="{{detail}}" btns="{{btns}}"/>
<Detail-two id="detail" wx:if="{{layoutType === 'typeMultiple'}}" detail="{{detail}}" btns="{{btns}}"/>
<Nodata code="{{retcode}}" bindrefresh="reloadData" />